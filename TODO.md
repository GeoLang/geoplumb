# TODO

- tensor as a first-rate pipeline type with tensor caps negotiation (glass2glass as a guide)
- geodukt executor swap onto the engine. prerequisites before it is worth doing: a fragment dissolve element (merge seam-split features by id), vector to vector elements, and a vector reproject adapter (the registered one is raster only)
